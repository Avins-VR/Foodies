{
  "homepage": "https://avins-vr.github.io/Foodies",
  "name": "Foodies",
  "version": "0.1.0",
  "private": true,
  "scripts": {
  "clean": "powershell Remove-Item -Recurse -Force .next; Remove-Item -Recurse -Force out",
  "build": "next build",
  "deploy": "npm run clean && npm run build && gh-pages -d out"
},
  "dependencies": {
    "bootstrap-icons": "^1.13.1",
    "next": "15.3.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
